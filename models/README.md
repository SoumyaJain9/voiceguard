# Model Storage Directory

Place your trained `best_model.onnx` weights in this directory.

- Default file path: `models/best_model.onnx`
- If the model is not found, the system runs with digital signal processing (DSP) acoustic feature analysis and heuristic classification.
